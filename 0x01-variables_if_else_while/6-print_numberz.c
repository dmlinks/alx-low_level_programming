#include <stdio.h>
/**
 * main - single base 10 digits from 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int m;

	m = 0;
	while
		(m < 10) {
			putchar(m + '0');
			m++;
		}
	putchar('\n');
	return (0);
}
