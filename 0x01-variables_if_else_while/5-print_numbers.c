#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 starting from 0
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
	char num;

	num = 0;
	while
		(num < 10) {
			putchar(num);
			num++;
		}
	putchar('\n');
	return (0);
}
