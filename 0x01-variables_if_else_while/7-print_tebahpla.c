#include <stdio.h>
/**
 * main - prints lowercase alphabet in reverse
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char w;

	w = 'z';
	while
		(w >= 'a') {
			putchar(w);
			w--;
		}
	putchar('\n');

	return (0);
}
